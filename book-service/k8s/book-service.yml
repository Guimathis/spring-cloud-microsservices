apiVersion: v1
kind: Service
metadata:
  name: book-service
  namespace: microservices
spec:
  type: LoadBalancer
  selector:
    app: book-service
  ports:
    - port: 8100
      targetPort: 8100
---
apiVersion: v1
data:
  EXCHANGE_SERVICE_URL: http://exchange-service:8000
kind: ConfigMap
metadata:
  name: book-service-config
  namespace: microservices
